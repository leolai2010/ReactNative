import React from 'react';
import { View } from 'react-native';

const CardSection = () => {
    return (
        <View></View>
    );
};

const style = {
    containerStyle: {
        
    }
}

export default CardSection;